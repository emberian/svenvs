.HOLMK/
.hol/
*Theory.sig
*Theory.sml
*Theory.dat
*.ui
*.uo
__pycache__/
*.pyc
.DS_Store
# Local output of release/build.sh (the freshly compiled envelope-console ELF
# + the downloaded CakeML compiler). The release workflow rebuilds it in CI.
release/_build/
