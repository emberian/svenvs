# The genealogy slice — the one gate applied to the root judge itself.
# Pure light HOL4. Its only svenvs dependency is the root certifierTheory,
# which is itself pure (list/pair/arithmetic): the general principle
# (soundness propagates along a forward-certified succession; only genesis
# + the strength-increase seam are irreducible) that the whole tower is an
# instance of. Reproducible by anyone with HOL4.
INCLUDES = ..
OPTIONS = QUIT_ON_FAILURE
